name: Update Marketplace Index

on:
  push:
    paths:
      - 'presets/**.json'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-index:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3
        with:
          ref: main # Pastikan kita checkout branch main, bukan detached HEAD
          fetch-depth: 0 # Ambil semua history biar aman buat rebase

      - name: Create Indexing Script
        run: |
          cat << 'EOF' > indexer.py
          import os
          import json
          import sys

          try:
              index_list = []
              repo = os.environ.get('GITHUB_REPOSITORY')
              branch = 'main'
              presets_dir = 'presets'
              output_file = 'marketplace.json'
              
              print(f"🚀 Starting indexing for {repo}...")

              if not os.path.exists(presets_dir):
                  print(f"⚠️ Warning: Folder '{presets_dir}' not found.")
              else:
                  for f in os.listdir(presets_dir):
                      if f.endswith('.json'):
                          try:
                              with open(os.path.join(presets_dir, f), 'r', encoding='utf-8') as file:
                                  data = json.load(file)
                                  meta = data.get('meta', {})
                                  meta['id'] = data.get('id', f.replace('.json', ''))
                                  meta['filename'] = f
                                  meta['download_url'] = f'https://raw.githubusercontent.com/{repo}/{branch}/{presets_dir}/{f}'
                                  
                                  if meta.get('name'):
                                      index_list.append(meta)
                                      print(f"✅ Indexed: {meta.get('name')}")
                          except Exception as e:
                              print(f"❌ Error reading {f}: {e}")

              index_list.sort(key=lambda x: x.get('created_at', 0), reverse=True)
              
              with open(output_file, 'w', encoding='utf-8') as f:
                  json.dump(index_list, f, indent=2)
                  
              print(f"🎉 Indexing complete! Total items: {len(index_list)}")

          except Exception as main_err:
              print(f"🔥 CRITICAL ERROR: {main_err}")
              sys.exit(1)
          EOF

      - name: Run Indexer
        run: python3 indexer.py

      - name: Commit and Push
        run: |
          git config --global user.name 'Flowork Bot'
          git config --global user.email 'bot@flowork.cloud'
          
          git add marketplace.json
          
          if git diff --staged --quiet; then
            echo "No changes to marketplace.json"
          else
            git commit -m "Auto-update marketplace index [skip ci]"
            
            # --- BAGIAN PENTING (THE FIX) ---
            # Tarik perubahan terbaru dari server dulu sebelum nge-push
            # Biar gak ditolak sama GitHub
            echo "🔄 Pulling latest changes..."
            git pull --rebase origin main
            
            echo "🚀 Pushing changes..."
            git push origin main
          fi
